// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

model Tutor {
    id         String @id @default(uuid())
    name       String
    experience String
    about      String
    photo      String
}

model Application {
    id    String @id @default(uuid())
    name  String
    phone String
}

model Review {
    id       String @id @default(uuid())
    name     String
    text     String
    approved String @default("Очікує на перевірку")
}
